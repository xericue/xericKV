defining the class of Chip8

16 registers (like logic design :O): use 16 registers that hold addresses
"dedicated location on a CPU for storage"

CHIP-8 has 16 8 bit registers, labeled V0 to VF; each may hold 0x00 to 0xFF
(V7 being special)

array initialization in C++ needs {} (just like any initialization):
uint8_t bruh[64] {};

