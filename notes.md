// while (true) {
    //     std::cout << "yea yea write your message already\n";
    //     std::cin >> message;
    //     write(tcp_socket, message, strlen(message));
    //     char buf[BUFSIZ] {};

    // ssize_t n = read(tcp_socket, buf, sizeof(buf));
    // if (n < 0) {
    //     std::cout << "read error\n";
    //     exit(1);
    // }

    // std::cout << "server says " << buf << '\n';
    // }
    
    // /*
    // std::cout << "yea yea write your message already\n";
    // std::cin >> message;
    
    // write(tcp_socket, message, strlen(message));
    // */
    

    // // char read_buffer[64] {};

    // // ssize_t n = read(tcp_socket, read_buffer, sizeof(read_buffer));
    // // if (n < 0) {
    // //     std::cout << "read error\n";
    // //     exit(1);
    // // }

    // // std::cout << "server says " << read_buffer << '\n';
    // close(tcp_socket);


kjhkjhflkfhjgkldfgkjl;fkj;ljf;lksjf;lajd;fad

// setting socket options to reuse sockets, beginning to create the struct 
    // for the socket address for correct connection
    // struct sockaddr_in {
    //     uint16_t sin_family;
    //     uint16_t sin_port;
    //     struct in_addr sin_addr;
    // };

    // struct in_addr {
    //     uint32_t s_addr;
    // };
    